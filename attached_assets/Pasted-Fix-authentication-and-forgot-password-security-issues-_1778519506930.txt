Fix authentication and forgot password security issues.

Current issues:

1. Forgot password is allowing password reset trials without proper account validation.
2. Old password still works even after password change.
3. Login is not properly validating password correctness.

Required fixes:

-----------------------------------
FORGOT PASSWORD FLOW
-----------------------------------

1. Before allowing password reset:
When user enters mobile number/email and clicks Forgot Password:

First check:
- Does account actually exist?

Valid accounts include:
- Under review
- Temp approved
- Approved
- Rejected
- Any registered profile

If account does NOT exist:
Show error:
"No account found with this mobile number"

Do NOT allow password reset flow for unregistered users.

2. OTP should send only after successful account verification.

-----------------------------------
PASSWORD CHANGE LOGIC
-----------------------------------

3. After password reset:
- Old password must immediately stop working
- Only new password should work

4. Ensure password is properly updated in:
- Authentication system
- Database/session/backend everywhere required

5. Invalidate old login sessions if needed.

-----------------------------------
LOGIN VALIDATION
-----------------------------------

6. Login must properly validate credentials:
- Wrong password → login should fail
- Show proper error:
  "Incorrect password"

7. Do NOT allow login with:
- Old password after reset
- Incorrect password
- Blank password

-----------------------------------
SECURITY + UX
-----------------------------------

8. Add proper backend validation:
- Never rely only on frontend checks
- Validate credentials securely server-side

9. Add loading/error states:
- Prevent multiple requests/spam taps

10. Keep UX smooth and beginner-friendly.

Goal:
Create secure and proper authentication behavior with correct password reset logic and real credential validation.